<?xml version="1.0" encoding="utf-8"?>
<!--
    Allows plain HTTP to the aircraft LAN (the Whisper server).
    HTTPS is still enforced for any public internet traffic.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
